# Third-party notice

`main.py` is the agent produced by the public Kaggle notebook
[ahmedberatozer/notebook865729c24e](https://www.kaggle.com/code/ahmedberatozer/notebook865729c24e),
downloaded verbatim from that kernel's output. Its SHA-256 matches the digest the
notebook asserts for its own build, so the file is exactly what the notebook publishes.

The file carries `SPDX-License-Identifier: Apache-2.0` and embeds the full licence text,
so unlike most pinned opponents this one is explicitly licensed for reuse. `ATTRIBUTION.md`
is the author's own credit list and is kept alongside it unchanged: the action tapes come
from `thomastschinkel`, the runtime and sell-lead ideas from `yhay81`, and the weed-repair,
room-guard, sale-clamp and dead-stock ideas from `tetsutani`.

This project uses the file in two ways. It is a benchmark opponent like every other bundle
here, and it is the upstream of the reactive chassis that `experiments/chassis_portfolio.py`
builds on, which the Apache-2.0 licence permits with attribution. Any artifact this project
ships from it keeps the licence header and the credit lines intact.
